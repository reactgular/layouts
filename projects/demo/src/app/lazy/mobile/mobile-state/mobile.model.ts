export interface MobileModel {
    /**
     * Show the filter side panel.
     */
    filterShow: boolean;

    /**
     * Show the footer panel.
     */
    footerShow: boolean;

    /**
     * Show the menu floating over top of content.
     */
    menuFloat: boolean;

    /**
     * Show the menu side panel.
     */
    menuShow: boolean;

    /**
     * Layout for mobile devices.
     */
    mobile: boolean;
}
